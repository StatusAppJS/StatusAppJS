import styled from "styled-components";

const StyledLoadingContainer = styled.div`
    display:block;
`;

const StyledHeader = styled.h1`
    text-align:center;
`;

const StyledSubHeader = styled.h2`
    text-align:center;
    cursor:pointer;
`;
export {StyledLoadingContainer, StyledHeader, StyledSubHeader};