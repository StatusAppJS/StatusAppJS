import styled from "styled-components";

const StyledLoadingContainer = styled.div`
    display: 'inline-flex';
    flex-direction: 'column';
`;

const StyledLoadingHeader = styled.h1`
    text-align:center;
`;

const StyledLoadingSubHeader = styled.h2`
    text-align:center;
`;
export {StyledLoadingContainer, StyledLoadingHeader, StyledLoadingSubHeader};